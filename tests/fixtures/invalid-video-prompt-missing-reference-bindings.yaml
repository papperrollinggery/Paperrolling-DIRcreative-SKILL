artifact:
  artifact_id: invalid-video-prompt-missing-reference-bindings
  version: 1.0.0
  status: fixture_invalid
  owner_skill: video-model-adapter

video_prompt_manifest:
  project_title: "Invalid Missing Reference Bindings"
  target_sequence_duration: "15s"
  longform_generation_mode: "single_sequence"
  visual_output_mode: "prompt_only"
  selected_reference_strategy: "hybrid"
  sequence_plan: ""
  sequence_ids:
    - invalid-seq-01
  reference_pack:
    - "invalid_board_01 identity reference"

execution_capabilities:
  image_generation:
    available: false
    provider: none
    allowed_by_user: false
  video_generation:
    available: false
    provider: none
    allowed_by_user: false
  file_write:
    available: true
  browser_review:
    available: false

reference_map:
  - ref_id: invalid_board_01
    file_or_generation_id: "prompt-only-board"
    role: "identity_reference"
    asset_output_status: prompt_ready
    use_for:
      - "identity consistency"
    do_not_use_for:
      - "showing the reference board"
    external_generation_instructions: "Generate externally before using as reference."

reference_bindings: []

model_prompts:
  seedance:
    enabled: true
    best_for: "fixture"
    prompt: "Reference board is guidance only; do not show labels, panels, arrows, grids, or board layout."
    reference_warning: "Do not show the reference board."
    audio_policy: "fixture audio"
    risk_notes: ["fixture"]
    retry_rules: ["fixture"]
  kling:
    enabled: true
    best_for: "fixture"
    prompt: "Reference board is guidance only; do not show labels, panels, arrows, grids, or board layout."
    reference_warning: "Do not show the reference board."
    audio_policy: "fixture audio"
    risk_notes: ["fixture"]
    retry_rules: ["fixture"]
  runway:
    enabled: true
    best_for: "fixture"
    prompt: "Reference board is guidance only; do not show labels, panels, arrows, grids, or board layout."
    reference_warning: "Do not show the reference board."
    audio_policy: "fixture audio"
    risk_notes: ["fixture"]
    retry_rules: ["fixture"]
  veo:
    enabled: true
    best_for: "fixture"
    prompt: "Reference board is guidance only; do not show labels, panels, arrows, grids, or board layout."
    reference_warning: "Do not show the reference board."
    audio_policy: "fixture audio"
    risk_notes: ["fixture"]
    retry_rules: ["fixture"]

universal_anti_misread_clause: |
  Do not show reference boards, labels, arrows, grids, or panels.

qa:
  video_prompt_gate: "fail"
  missing_audio_policy: false
  missing_reference_map: false
  visual_output_mode_valid: true
  capability_receipt_present: true
  missing_generated_assets_blocked: true
  longform_sequence_refs_valid: true
  conflicting_actions: []
  model_prompts_differ_materially: false
  reference_bindings_present: false
  selected_reference_strategy_present: true
